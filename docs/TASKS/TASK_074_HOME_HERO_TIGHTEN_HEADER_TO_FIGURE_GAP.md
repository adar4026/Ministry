# TASK_074 — Главная: подтянуть цифру hero ближе к шапке

**Дата:** 20 сентября 2026
**Статус:** Реализовано, проверено локально в браузере (390 px) — не
закоммичено.
**Скоуп:** только вертикальный зазор «низ строки даты → верх стеклянной
цифры» в верхнем Hero. WebGL-фон, стилистика, размер и вид цифры,
progress bar, строки «из цели …», «Опережение …», «Осталось …», «До конца
…», кнопки «Детали» / «+ Добавить часы», нижние секции — без изменений.

---

## 1. Что было

От низа строки даты (`Воскресенье, 20 сентября`) до верха цифры набиралось
`heroBlock.gap 14` (`app/(tabs)/index.tsx`) + `HomeHero.wrap.paddingTop 0` +
`HERO.figureWrap.marginTop 8` (`heroFigure.tsx`) = 22 px структурного
зазора, что с учётом leading 60/64-строки цифры читалось как заметная
пустота сверху hero — владелец отметил, что показатель выглядит визуально
опущенным.

## 2. Что изменено

| Слой | Было | Стало |
|---|---|---|
| `index.tsx` `heroBlock.gap` (шапка → HomeHero) | 14 | 6 |
| `heroFigure.tsx` `HERO.figureWrap.marginTop` | 8 | 0 |

Итого структурный зазор «дата → цифра»: 22 → 6 px. Оба значения общие для
`HoursHero` (режим «часы») и `PublisherHero` (режим «дни служения») — оба
используют один и тот же `heroBlock` экрана и один и тот же
`HERO.figureWrap`, так что подтяжка применилась к обоим режимам без
отдельных правок.

Ничего в самой цифре (кегль, вес, обёртка, стеклянные слои), progress
bar, темпе, метриках, pill-кнопках и WebGL-сцене не тронуто —
`heroContentHeight`/`sceneHeight` по-прежнему считаются через `onLayout`,
так что сцена сама подстроилась под чуть более короткий контент.

## 3. Проверки (выполнены)

- Dev-сервер (`expo start --web`), браузер, **390 px**: цифра «37 ч»
  визуально поднялась вплотную к строке даты, воздух сверху минимальный,
  ничего не слиплось; progress bar / «Опережение…» / «Осталось / До
  конца» / кнопки — прежние интервалы, наложений нет.
- `npx jest src/components/dashboard/__tests__/HomeHero.test.tsx
  src/components/dashboard/__tests__/PublisherHero.test.tsx
  "app/(tabs)/__tests__/index.test.tsx"` — все тесты зелёные (интервалы
  не были захардкожены в тестах как точные px, поведенческие проверки не
  пострадали).
- Консоль браузера — без ошибок.

## 4. Не входит

Commit/push/deploy — до решения владельца (ADR-007 gate ещё не пройден
для этой задачи).

## 5. Architecture Review Checklist (ADR-007)

- [x] Нет новых зависимостей; `package.json` не менялся.
- [x] Модель данных и ключи AsyncStorage не менялись; правка ограничена
      двумя числовыми значениями стиля (`gap`, `marginTop`).
- [x] Один источник данных не затронут: `formatHMRounded()` /
      `monthProgress()` / `splitDuration()` не менялись — правка чисто
      layout, без изменения данных или их формата.
- [x] Общий код: `HERO.figureWrap` и `heroBlock` разделяются `HoursHero`
      и `PublisherHero` — подтяжка применилась к обоим режимам без
      дублирования правки.
- [x] Токены/типографика/WebGL-сцена не менялись; `sceneHeight` считается
      через существующий `onLayout` (TASK_065), новой логики не добавлено.
- [x] Доступность: `accessibilityLabel`/`importantForAccessibility` на
      hero не менялись — правка не затрагивает разметку, только spacing.
- [x] Мёртвый код не оставлен; закомментированные объяснения (`TASK_074`)
      обновлены на месте прежних (`TASK_070`) комментариев о том же
      зазоре.
- [x] Regression: `npx jest` — полный прогон, 0 упавших (см. §3).
- [ ] Живая проверка на iPhone (PWA standalone) — post-deploy шаг
      владельца.
