# TASK_075 — «Ближайшие события»: опустить header на 14 px

**Дата:** 20 сентября 2026
**Статус:** Implemented / deployed / production verified / owner
approved. Закоммичено (`958a6d2`), запушено, задеплоено (`gh-pages`
`c551264`), проверено на production, подтверждено владельцем.
**Скоуп:** только вертикальное положение header-блока экрана
`app/upcoming-events.tsx` (круглая кнопка «назад» + заголовок). Размер
кнопки, размер/стиль заголовка, шрифты, карточки событий, интервалы
внутри карточек, цвета, тени, фон, данные и порядок событий — без
изменений.

---

## 1. Что было

`header` (`app/upcoming-events.tsx`) сидел вплотную к safe-area top:
`SafeAreaView` (`edges: ["top", "left", "right"]`) добавляет только сам
safe-area inset, `header` — `height: 48, justifyContent: "center",
paddingHorizontal: 16`, без какого-либо доп. отступа сверху. На узком
экране (390 px) кнопка «назад» и заголовок оказывались практически под
статус-баром — по кнопке неудобно попадать.

## 2. Что изменено

`header` получил `marginTop: 14` — единственная правка:

```
header: {
  height: 48,
  marginTop: 14,
  justifyContent: "center",
  paddingHorizontal: 16,
},
```

`marginTop` (а не `paddingTop`) сдвигает весь блок как единое целое, не
трогая внутреннее центрирование кнопки и заголовка друг относительно
друга — оба остаются на той же взаимной оси, просто ниже. Значение не
заменяет safe-area расчёт, а добавляется поверх него (`SafeAreaView`
по-прежнему считает реальный inset устройства) — итоговая формула
`safeAreaTop + 14`, а не жёсткая абсолютная позиция. `ScrollView` с
карточками идёт следующим элементом в том же потоке — сдвигается вниз
вместе с header, `content.paddingTop: 4` (отступ первой карточки от
header) не менялся, так что взаимный зазор header → первая карточка
сохранён.

Значение 14 — середина запрошенного диапазона 12–16 px; выбрано и
визуально подтверждено на 390 px (см. §3).

## 3. Проверки (выполнены)

- Dev-сервер (`expo start --web`), браузер, **390 px** (safe-area
  inset = 0 в вебе, поэтому сдвиг виден «в чистом виде»): кнопка «назад»
  и заголовок опустились на 14 px как единый блок, взаимное выравнивание
  не изменилось; замер `getBoundingClientRect()` заголовка — `top: 27.25`
  (было `13.25` при `marginTop: 0`), разница ровно 14 px.
- Первая карточка («Нет предстоящих событий») опустилась вместе с
  header, отступ от заголовка визуально тот же.
- `npx jest app/__tests__/upcomingEventsScreen.test.tsx` — 4/4 зелёные
  (заголовок, back-контрол, пустое состояние, полный список — поведение
  не изменилось, тест не завязан на конкретные px).
- `npx tsc --noEmit` — чисто.
- Консоль браузера — без ошибок.
- Полный релизный прогон перед коммитом: `tsc --noEmit` чисто, `npx jest`
  — **86/86 suites, 1307/1307 tests**, `npx expo export --platform web`
  собирается, `git diff --check` / `git diff --cached --check` чисты.
- Push в `origin/main` (`958a6d2`), `npm run deploy` → `gh-pages`
  `c551264`, локальный бандл `entry-e4cd3114d50a8a890ff28a58fd1e89f1.js`.
- Production, свежая вкладка без кэша (`?nocache=2`): опрос
  `adar4026.github.io` совпал с локальным хэшем со второй попытки
  (~20 c CDN-задержки), network-лог вкладки подтвердил фактическую
  загрузку именно этого `entry-*.js`. На 390 px `getBoundingClientRect()`
  заголовка дал `top: 27.25` — идентично локальной сборке (было `13.25`
  до правки) — сдвиг ровно +14 px воспроизвёлся на production один в
  один; кнопка «назад» и заголовок опустились как единый блок, взаимное
  выравнивание не изменилось; первая карточка последовала за header с
  прежним зазором; консоль без ошибок.

## 4. Итог

Подтверждено владельцем. Commit `958a6d2` → push → deploy `gh-pages`
`c551264` выполнены, production проверен в свежей вкладке без кэша.

## 5. Не входит

Живая проверка реального safe-area inset на iPhone (нотч/Dynamic Island)
— веб-preview safe-area эмулирует как 0, поэтому реальное значение на
устройстве не проверялось в рамках этой задачи; формула
`safeAreaTop + 14` рассчитана на то, что `SafeAreaView` подставит
настоящий inset устройства.

## 6. Architecture Review Checklist (ADR-007)

- [x] Нет новых зависимостей; `package.json` не менялся.
- [x] Данные и ключи AsyncStorage не менялись; правка — один стиль
      (`marginTop`) в одном экране.
- [x] Safe-area pattern сохранён: `SafeAreaView` (`edges: top/left/right`)
      по-прежнему единственный источник инсета; добавленный отступ —
      визуальный, поверх него, не подменяет его и не хардкодит абсолютную
      позицию.
- [x] Внутреннее выравнивание кнопки и заголовка не тронуто —
      `justifyContent: "center"` внутри `header` не менялся, сдвинут сам
      контейнер.
- [x] Карточки событий (`UpcomingEventRow`, `SummaryCard`), интервалы
      внутри них, цвета, тени, фон (`HomeBackground`) — не менялись;
      правка не затрагивает `content`/`list`/`card` стили.
- [x] Доступность: `BackButton`/заголовок — те же `accessibilityLabel`/
      `pointerEvents`, что и раньше.
- [x] Мёртвый код не оставлен.
- [x] Regression: релевантный jest-suite зелёный (см. §3).
- [ ] Живая проверка на iPhone (реальный safe-area inset, Dynamic
      Island) — post-deploy шаг владельца.
