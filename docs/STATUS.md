# STATUS — Ministry

_Последнее обновление: TASK_009 (Public Seed Privacy Remediation) реализована
на `main` локально, **не выпущена, не запушена**; последний тег — v0.4.4
(16 июля 2026); TASK_008 реализована ранее, коммит `073cd02`, также не
выпущена_

---

## Текущий статус: Hours 2.0 завершён (005A–005E), Главная переработана (TASK_007), TASK_008 (integrity fixes) и TASK_009 (privacy remediation) реализованы локально и не выпущены

**Завершённые задачи:** TASK_001 · TASK_002 · TASK_003 · TASK_004 ·
TASK_005A · TASK_006 · TASK_005B · TASK_005C · TASK_007 · TASK_005D ·
TASK_005E · TASK_008 · TASK_009.

### TASK_009 — Public Seed Privacy Remediation (реализована локально)

- `src/data/seed.js`: `SEED_RECORDS`, `SEED_EVENTS`, `SEED_TALKS` заменены на
  пустые массивы (`[]`); интерфейс модуля и `seed.ts` не изменялись.
  Личный набор данных пользователя (260 записей часов, 25 событий, 6 речей)
  извлечён в верифицированную приватную резервную копию **вне репозитория**
  перед изменением — см. `docs/TASKS/TASK_009_PUBLIC_SEED_PRIVACY_REMEDIATION.md`
  §4.
- `prototype/App.jsx` удалён из отслеживаемого дерева — не имел активных
  импортов, дублировал те же личные маркеры.
- Текущее exposure дерева `main` (layer 1, см. TASK_009 §3) — **устранено**.
  Будущие production-экспорты (`expo export --platform web`) больше не
  включают личный seed-набор, что подтверждено проверкой скомпилированного
  бандла на предмет Unicode-escaped маркеров (TASK_009 §13).
- **Не устранено этой задачей (осознанно, вне области):** исторические
  коммиты Git (layer 2) по-прежнему содержат прежнее содержимое `seed.js` и
  `prototype/App.jsx` в полном объёме — требуется отдельная, одобренная
  владельцем задача перезаписи истории (`git filter-repo`/BFG или аналог).
  Копии у третьих лиц (форки, поисковые индексы, Wayback Machine),
  сделанные до этого коммита, вне контроля этой задачи.
- Действующий (live) GitHub Pages деплой (устарел с 2026-07-14) **не
  обновлён** этой задачей — по-прежнему отдаёт то, что было задеплоено
  последним, пока не пройдёт новый `npm run deploy`. TASK_009 не выполняла
  push, deploy, bump версии или создание тега.
- **TASK_010** зарезервирован за будущим редизайном Hours UX (включая
  судьбу `MonthlyHoursCard.tsx`) — не начат, не определён этой задачей,
  только номер закреплён (TASK_009 §18).

**Архитектура Hours 2.0** (`docs/TASKS/TASK_005_ARCHITECTURE.md`) заморожена
(FROZEN) и одобрена. Architecture Review Checklist (ADR-007) применялся к
TASK_005A/005B/005C — все прошли успешно. Для TASK_005D и TASK_005E
прохождение checklist **не было задокументировано** на момент их релиза;
ретроспективное ревью выполнено в рамках TASK_008 (см. раздел ниже) и
**нашло реальные дефекты корректности/архитектуры**, которые TASK_008
устранил постфактум на неопубликованном `main`. Гейт ADR-007 для
TASK_005D/005E задним числом **не считается пройденным** — это
зафиксировано как исторический факт, не переписывается.

### Инцидент UTF-8 (июль 2026) — устранён

При реализации TASK_005D/005E через маршрут Nemotron (Hermes Gateway) все
русские строки были повреждены (mojibake) — дефект декодирования SSE в
шлюзе, см. ADR-008 и `docs/AI_INFRASTRUCTURE.md` §8. Повреждение вошло в
коммиты `b8d1a4d` и `fc23c52`; русский интерфейс полностью восстановлен в
коммите `b9fe019` и включён в тег **v0.4.4** (коммит `88769b7`) — это уже
опубликованный, освобождённый релиз. Остаточный mojibake, обнаруженный
отдельно в `src/data/stats.ts` (задел, не связанный с самим UI-инцидентом),
устранён в рамках TASK_008.

### Навигация / экраны

Нижние вкладки (5): **Главная** · **Часы** · **Добавить** · **События** ·
**Профиль**. Вкладки «Речи» не существует с TASK_003 — публичные речи
показываются внутри единой ленты «События» (фильтр «Публичные речи»),
данные остаются в отдельной коллекции `talks`.

- `app/(tabs)/index.tsx` — Главная: редизайн TASK_007 («Христианская
  жизнь»), dashboard-компоненты в `src/components/dashboard/`.
- **`app/(tabs)/hours/` — вложенный модуль «Часы» (TASK_005D):**
  - `index.tsx` — Hours Dashboard (карточка месяца, быстрые действия,
    тепловая карта служебного года, сетка месяцев);
  - `entry.tsx` — Manual Entry: создание/редактирование `Session`
    (перенесён из `app/entry.tsx` в TASK_005D);
  - `history.tsx` — История: список `Session` по служебным годам
    (перенесён из `app/history.tsx`);
  - `timer.tsx` — Ministry Timer (перенесён из `app/timer.tsx`,
    TASK_005C);
  - `stats.tsx` — Статистика (TASK_005E): текущий месяц, служебный год,
    тренд 12 месяцев, темп, прогноз, тепловая карта;
  - `month/[key].tsx` — Детали месяца (сессии дня, тепловая карта дней);
  - `_layout.tsx` — вложенный Stack-навигатор.
- `app/(tabs)/add.tsx` — Добавить (ручной ввод часов/событий/речей).
- `app/(tabs)/timeline.tsx` — События (объединённая лента событий и речей).
- `app/(tabs)/profile.tsx` — Профиль (личные данные, версия приложения).
- `app/service.tsx` — легаси-маршрут сохранён как compatibility redirect к
  `/hours`.

### Архитектура и модель данных

- Expo + React Native (expo-router, TypeScript), Expo SDK 54.
- Единый `StoreContext` — вся работа с данными идёт только через него.
- **Session-first агрегация (TASK_005A, канонизирована в TASK_008):** для
  каждого месяца авторитетный источник ровно один — по **наличию**
  `Session` за месяц (не по знаку суммы), иначе легаси `HourRecord`.
  Источники никогда не смешиваются. `monthTotal()` и `sessionsForMonth()`
  живут в `src/data/stats.ts` (единственная реализация, приватный helper
  `resolveMonthTotal()`) и переэкспортируются без изменения сигнатур из
  `src/data/constants.ts`, откуда их импортируют все остальные модули;
  `serviceYearAggregation()` остаётся в `constants.ts`.
- **Статистика (TASK_005E):** чистые функции в `src/data/stats.ts`;
  компоненты в `src/components/stats/` (MonthlyStatsCard,
  ServiceYearStatsCard, TrendChart, PaceCard, ProjectionCard) +
  `src/components/HeatMap.tsx`. Hours Dashboard (`app/(tabs)/hours/index.tsx`)
  и HeatMap больше не агрегируют `records`/`sessions` напрямую (TASK_008) —
  оба потребляют `monthCellsForSY()`; HeatMap разрешает каждую ячейку по её
  собственной дате `YYYY-MM`, а не по одному общему году сентябрьской ячейки.
- **Правило блокировки легаси-записи (TASK_005B):** единая точка
  проверки — `legacyEntryBlockReason()` в `src/data/constants.ts`;
  легаси-форма отказывает для текущего/будущих месяцев и прошлых месяцев
  с сессиями, с явным пояснением в форме.
- **Таймер (TASK_005C):** `TimerState` — управляющее состояние
  (crash-recovery), не отчётная сущность; ключ `mj_timer_v1`; логика —
  `src/data/timer.ts`, жизненный цикл — `src/hooks/useTimer.ts`.

### Хранилище (AsyncStorage)

| Ключ | Содержимое | Статус |
|------|-----------|--------|
| `mj_records_v1` | `HourRecord[]` — легаси месячные итоги | Заморожен, не изменяется |
| `mj_events_v1` | `MinistryEvent[]` | Активен |
| `mj_talks_v1` | `Talk[]` | Активен |
| `mj_sessions_v1` | `Session[]` — гранулярная запись времени (TASK_005A) | Активен; живых пользовательских записей пока 0 — это ожидаемо |
| `mj_timer_v1` | `TimerState` — состояние таймера (crash-recovery) | Не отчётная сущность; в агрегацию не входит |

### Тестирование

- Инфраструктура юнит-тестов: `jest` + `jest-expo` (`npm test`).
- **117 тестов, 10 файлов:** агрегационный слой, CRUD сессий в
  `StoreContext`, рендер-тесты `TodayCard`, `RecordForm` (4 сценария
  блокировки), `SessionForm`, группировка «Истории» (`buildHistory`),
  таймер (чистые функции + интеграционный тест `useTimer × StoreContext`),
  слой статистики TASK_005E (тренд, темп, прогноз, ячейки тепловой карты).
- **TASK_008 (+12 тестов):** существование `Session` при нулевой сумме
  как критерий авторитетности (не сумма) — `src/data/__tests__/aggregation.test.ts`;
  граничные месяцы служебного года (сентябрь/декабрь начального года,
  январь/август следующего) с проверкой значений, не только меток —
  `src/data/__tests__/stats.test.ts`; прямой тест функции сопоставления
  ячеек тепловой карты `resolveMonthGridCells()` — новый файл
  `src/components/__tests__/HeatMap.test.ts`.
- `npx tsc --noEmit` — чисто.

### Развёртывание и версии

- GitHub репозиторий: `adar4026/Ministry` (публичный — см. ниже).
- Веб-версия (Expo Web / PWA) на GitHub Pages:
  **https://adar4026.github.io/Ministry/**
  (`npm run deploy` → `expo export --platform web && gh-pages -d dist --nojekyll`).
- **Состояние релиза (актуально после TASK_008):** последний тег —
  **`v0.4.4`**, коммит `88769b7`, включает восстановление UTF-8
  (`b9fe019`). Коммит `de32d87` (редирект `/service` → `/hours`) и все
  коммиты TASK_008 (постановка задачи, реализация `073cd02`,
  документационное согласование) идут **после** `v0.4.4` и
  **не выпущены** — `main` опережает последний тег. TASK_008 **не
  выполняла** деплой, push, bump версии или создание тега — это осталось
  на усмотрение владельца после проверки.
- Версия приложения: `package.json` = `0.4.4`, `package-lock.json`
  (top-level и `packages[""]`) синхронизированы на `0.4.4` в рамках
  TASK_008 (только метаданные версии, без изменений зависимостей). Поле
  `expo.version` в `app.json` (`1.0.0`) — отдельное, для нативных сборок;
  веб/PWA-пайплайном не используется.

### ⚠️ Важно: репозиторий публичный

Репозиторий `adar4026/Ministry` — **public** (требование GitHub Pages на
бесплатном плане). Аутентификации на сайте нет. С TASK_009 текущий
`src/data/seed.js` в отслеживаемом дереве содержит только пустые массивы —
личные данные пользователя туда не попадают. **Историческая оговорка:**
предыдущие коммиты Git по-прежнему содержат прежнее (непустое) содержимое
`seed.js`, доступное через `git log`/`git show` любому, кто клонирует
репозиторий — это отдельная, ещё не одобренная задача (см. TASK_009 §16).

### Что НЕ работает / не проверено

- **Публичный PWA без разграничения доступа — риск приватности не
  устранён.** Сайт полностью публичный, аутентификации нет; TASK_008 не
  затрагивала этот вопрос (вне области задачи).
- **Мобильная сборка (Expo Go, SDK 54) не подтверждена живым запуском на
  устройстве** — ни ранее, ни в рамках TASK_008. Таймер и модуль «Часы»
  не проверялись на реальном iPhone.
- **Ретроспективное ревью для TASK_005D/TASK_005E выполнено в рамках
  TASK_008; его вывод — гейт ADR-007 задним числом не считается
  пройденным** для этих двух задач (см. раздел «Текущий статус» выше и
  `docs/TASKS/TASK_008_HOURS_INTEGRITY_AND_RELEASE_RECONCILIATION.md`
  §12). Обнаруженные технические дефекты (§2.1–§2.7 того документа)
  устранены TASK_008 на неопубликованном `main` — сам факт непройденного
  гейта на момент релиза TASK_005D/TASK_005E задним числом не
  переписывается.
- **`src/components/MonthlyHoursCard.tsx` — подтверждённо orphaned**
  (нет ни одной ссылки в `app/` или `src/` кроме собственного файла).
  Сознательно не удалён в TASK_008 — отложен до будущего редизайна Hours
  UX, так как удаление может затронуть плановый редизайн экрана.
- Редизайн экранов «Часы» и «Главная» — запланирован отдельной будущей
  задачей, вне области TASK_008.

### Ближайший следующий шаг

1. Push коммитов TASK_008 (документ, реализация, документационное
   согласование) — по решению владельца.
2. Живая проверка приложения (модуль «Часы», таймер, HeatMap) в Expo Go
   на iPhone — не выполнялась ни ранее, ни в TASK_008.
3. Решение владельца: деплой веб-версии / новый тег (следующий за
   v0.4.4) — после прохождения Architecture Review Checklist (ADR-007)
   для содержимого самого TASK_008.
4. Вопрос приватности публичного PWA — отдельная задача, не начата.
5. Редизайн Hours UX (включая судьбу `MonthlyHoursCard.tsx`) — отдельная
   будущая задача, не начата.
6. `AGENTS.md` (governance-файл для Codex, зеркало `CLAUDE.md`) —
   добавление в репозиторий отложено как отдельный опциональный этап,
   не связанный с функциональной работой TASK_008.

> Примечание: расхождение ключей хранилища устранено ещё в TASK_001.
> Веб-прототип использовал `sj_*_v7`; приложение следует контракту
> ARCHITECTURE.md — `mj_*_v1` (плюс `mj_sessions_v1` с TASK_005A и
> `mj_timer_v1` с TASK_005C).

---

## Данные

Историческая информация пользователя (записи часов, события, публичные
речи) не воспроизводится в публичной документации проекта — ни числовыми
таблицами, ни отдельными записями, ни датами, ни примечаниями. Начиная с
TASK_009:

- реальные данные пользователя авторитетно хранятся локально на устройстве,
  в AsyncStorage (см. «Хранилище (AsyncStorage)» выше) — это единственный
  источник истины после первого запуска;
- верифицированная приватная резервная копия существует **вне
  репозитория**; её путь и контрольная сумма намеренно не публикуются в
  этом документе;
- публичные seed/source-файлы (`src/data/seed.js`) содержат только пустые
  массивы для первого запуска и не восстанавливаются из приватной резервной
  копии в публичное дерево исходников (TASK_009).
